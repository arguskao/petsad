---
import { navLinks, site } from '../data/site';
---

<nav class="navbar">
  <div class="container">
    <div class="nav-wrapper">
      <a class="logo" href="/" aria-label={site.name}>
        <svg width="40" height="40" viewBox="0 0 40 40" fill="none" aria-hidden="true">
          <circle cx="20" cy="20" r="18" fill="#FF6B6B" opacity="0.2" />
          <path d="M12 18C12 18 10 14 12 12C14 10 16 12 16 12C16 12 18 10 20 10C22 10 24 12 24 12C24 12 26 10 28 12C30 14 28 18 28 18C28 18 32 20 32 24C32 28 28 32 20 32C12 32 8 28 8 24C8 20 12 18 12 18Z" fill="#FF6B6B" />
          <circle cx="16" cy="20" r="2" fill="white" />
          <circle cx="24" cy="20" r="2" fill="white" />
        </svg>
        <span>{site.name}</span>
      </a>

      <div class="nav-links">
        {navLinks.map((link) => (
          <a href={link.href}>{link.label}</a>
        ))}
      </div>

      <div class="nav-actions">
        <a class="btn-secondary favorites-chip" href="/favorites">
          收藏
          <span class="favorite-count-pill" data-favorite-count>0</span>
        </a>
        <a class="btn-secondary" href="/member">我的會員</a>
        <a class="btn-primary" href="/apply">{'開始領養'}</a>
      </div>
    </div>
  </div>
</nav>
