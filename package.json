{
    "name": "saas-landing-page",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "astro dev",
        "dev:astro": "astro dev",
        "dev:pages": "wrangler pages dev ./dist --port 8787",
        "dev:legacy": "wrangler pages dev --port 8787",
        "deploy": "wrangler pages deploy ./dist",
        "deploy:astro": "wrangler pages deploy ./dist",
        "deploy:legacy": "wrangler pages deploy ./public",
        "build": "astro build && tsc",
        "build:astro": "astro build",
        "preview:astro": "astro preview"
    },
    "dependencies": {
        "hono": "^4.0.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20260612.1",
        "astro": "^6.0.0",
        "typescript": "^5.3.3",
        "wrangler": "^4.100.0"
    }
}
